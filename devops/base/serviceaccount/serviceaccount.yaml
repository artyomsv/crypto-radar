apiVersion: v1
kind: ServiceAccount
metadata:
  name: projectr-x-sa
# If you pull images from a private registry, add an imagePullSecrets entry,
# e.g.:
#   imagePullSecrets:
#     - name: registry-credentials
# and create that Secret out-of-band (NEVER commit it).
