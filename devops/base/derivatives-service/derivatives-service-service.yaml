apiVersion: v1
kind: Service
metadata:
  name: derivatives-service
  labels:
    app.kubernetes.io/name: derivatives-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8085
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: derivatives-service
